const DeleteTask = ({ id }) => {
  return (
    <button className="mt-4 rounded bg-red-500 px-4 py-2 text-white transition hover:bg-red-600">
      Delete Tasks
    </button>
  );
};

export default DeleteTask;
